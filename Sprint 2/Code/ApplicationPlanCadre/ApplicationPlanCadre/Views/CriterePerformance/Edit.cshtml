@model ApplicationPlanCadre.Models.CriterePerformance

@{
    ViewBag.Title = "Modification d'un critère de performance";
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    @Html.HiddenFor(m => m.numero)
    @Html.HiddenFor(m => m.idElement)

    <div class="container">
        <div class="form-box">

            <header class="light-header">

                <h1>@ViewBag.Title</h1>

            </header>

            <div class="form-group">
                @Html.TextAreaFor(m => m.description, new { @rows = "3", @maxlength = "300", @class = "form-control" })
                @Html.ValidationMessageFor(m => m.description, "", new { @class = "text-danger" })
            </div>

            <input type="submit" value="Confirmer" class="fancy-button" />
            <a href='@Url.Action("Create", new { idElement = Model.idElement })' class="fancy-button rouge">Annuler</a>

        </div>
    </div>

}